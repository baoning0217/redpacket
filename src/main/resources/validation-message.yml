redpacket:
  userId:
    notnull: 用户id不能为空


# 几个相关注解
# @Size : 表示一个字符串的长度或者一个集合的大小，必须在某一个范围之内; min参数表示范围的下限，max参数表示范围的上限，message表示校验失败的提示信息
# @NotNull : 表示该字段不能为空
# @DecimalMin : 表示对应属性的下限;  @DecimalMax : 表示对应属性的上限
# Email : 表示对应属性格式是一个Email